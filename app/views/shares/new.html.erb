<h1>Share Passwords</h1>

<%= form_with model: [@password, @user_password], url: password_shares_path(@password) do |f| %>

  <div>
    <%= f.label :user_id %>
    <%= f.collection_select :user_id, @users, :id, :email %>
  </div>

  <div>
    <%= f.submit "Share" %>
  </div
<% end %>
